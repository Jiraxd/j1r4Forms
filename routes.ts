/*
- Public routes that are accessible to the public by default
*/
export const publicRoutes = [
    "/"
]

export const authRoutes = [
    "/auth/login",
    "/auth/register"
]

export const apiAuthPrefix = "/api/auth";

export const defaultRedirect = "/";